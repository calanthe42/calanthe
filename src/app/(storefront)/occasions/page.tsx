import { StubPage } from "@/components/blocks/StubPage";

export const metadata = { title: "Occasions" };

export default function Page() {
  return <StubPage eyebrow="Occasions" title="Every occasion, composed." />;
}
