import { StubPage } from "@/components/blocks/StubPage";

export const metadata = { title: "Shop" };

export default function Page() {
  return <StubPage eyebrow="Shop" title="The collection is being gathered." />;
}
