import { StubPage } from "@/components/blocks/StubPage";

export const metadata = { title: "Delivery Information" };

export default function Page() {
  return (
    <StubPage
      eyebrow="Help"
      title="Delivery Information"
      note="Same-day delivery across the UAE for orders placed before 5pm. Full policy copy arrives with the content phase."
    />
  );
}
