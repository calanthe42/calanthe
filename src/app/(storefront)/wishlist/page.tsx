import { StubPage } from "@/components/blocks/StubPage";

export const metadata = { title: "Wishlist" };

export default function Page() {
  return <StubPage eyebrow="Wishlist" title="Kept close to heart." />;
}
